<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%
	String strcnt = (String) application.getAttribute("counter"); // getAttribute()함수를 사용하여 "counter"속성 값을 가져온다. 기본적으로는 object 형태로 값을 반환하므로 String으로 변환한다.
	int count = (strcnt==null) ? 0: Integer.valueOf(strcnt); //처음에는 strcnt가 null이므로 0반환  그 다음부턴 strcnt밸류값을 정수로 변환해서 반환
	out.print("방문자수 : " +count);
	application.setAttribute("counter",String.valueOf(++count));







%>